[package]
name = "simple_package"
version = "0.1.0"

[dependencies]
starknet = "2.1.0"
snforge_std = {  git = "https://github.com/foundry-rs/starknet-foundry.git", branch = "411-test" }

[[target.starknet-contract]]
sierra = true
casm = true

[tool.snforge]
exit_first = false
